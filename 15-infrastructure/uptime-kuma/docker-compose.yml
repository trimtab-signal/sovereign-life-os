services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptime-kuma
    ports:
      - "${PORT:-3001}:3001"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
